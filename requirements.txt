# --- Setup ---
#  python3 -m venv .venv
#  source .venv/bin/activate
#  Windows:  .venv\Scripts\activate
#  python -m pip install -U pip
#  python -m pip install -r requirements.txt
#
# This file pulls PyTorch CUDA 12.4 builds (useful for Linux/Windows + NVIDIA).
# For CPU-only: install torch first, e.g.
#    python -m pip install torch --index-url https://download.pytorch.org/whl/cpu
#   then run  python -m pip install transformers datasets pandas scikit-learn sentencepiece tqdm
#   (or temporarily remove the two lines  --extra-index-url ...  and  torch  below, then re-add only the other packages).
# On Apple Silicon, PyPI or CPU wheels for torch usually include MPS support.
#
# Python 3.10+ recommended.
# ---

--extra-index-url https://download.pytorch.org/whl/cu124
torch
transformers
datasets
pandas
scikit-learn
sentencepiece
tqdm
